
# coding: utf-8

# In[8]:

def max_gain(self):
    if val1:
        if val1.max_gain(self)[1] > val0.max_gain(self)[1]:
            return val1.max_gain(self)
    if attributes:
        return (this, max([heuristic(attribute) for attribute in attribute]))
    else:
        return (None, 0)


# In[ ]:

def split(self, attribute):
    val0 = Node(self.atributes.remove(attribute), self.ancestors.append(attribute_value(attribute, 0)), 
                self.data, self.heuristic)
    val0 = Node(self.atributes.remove(attribute), self.ancestors.append(attribute_value(attribute, 1)), 
                self.data, self.heuristic)

